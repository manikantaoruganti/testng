import React from 'react';
// This component would typically overlay a canvas or SVG with dynamic heat data
// For now, it's a placeholder.
const HeatMap = () => {
  return (
    <div className="w-full h-full bg-surface rounded-xl flex items-center justify-center text-textSecondary">
      <p>Heat Map Overlay (Future Feature)</p>
    </div>
  );
};

export default HeatMap;

